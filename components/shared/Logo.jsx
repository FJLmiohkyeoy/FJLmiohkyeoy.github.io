import styled from "styled-components";

const Container = styled.div`
    font-size: 1.5rem;
    font-weight: blod;
`;

const Logo = () => {
    return (
        <Container>Jihyo</Container>
    );
}

export default Logo;